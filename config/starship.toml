add_newline = true
scan_timeout = 30 # timeout for file scan
command_timeout = 1000

# プロンプトの表示順序
format = """
$username\
$directory\
$git_branch\
$git_status\
$aws\
$gcloud\
$docker_context\
$nodejs\
$python\
$rust\
$golang\
$cmd_duration\
$line_break\
$character"""

[character]
success_symbol = "[>](bold green)"
error_symbol = "[x](bold red)"
vimcmd_symbol = "[<](bold green)"

[directory]
truncation_length = 5
truncate_to_repo = false
truncation_symbol = "…/"

[git_branch]
symbol = " "
format = "[$symbol$branch]($style) "

[git_status]
format = '([\[$all_status$ahead_behind\]]($style) )'

[cmd_duration]
min_time = 2000  # 2秒以上で表示
format = "took [$duration]($style) "

[aws]
format = 'on [$symbol($profile )(\($region\) )]($style)'
style = "bold blue"

[aws.region_aliases]
ap-northeast-1 = "jp"

[gcloud]
disabled = true
# symbol = '️🇬️ '
# format = '[$symbol$active]($style) '
# style = 'bold yellow'

[docker_context]
format = "via [🐋 $context](blue bold)"

[nodejs]
symbol = " "
format = "via [$symbol($version )]($style)"

[python]
symbol = " "
format = "via [$symbol($version )]($style)"

[rust]
symbol = " "
format = "via [$symbol($version )]($style)"

[golang]
symbol = " "
format = "via [$symbol($version )]($style)"

[username]
style_user = "white bold"
style_root = "black bold"
format = "[$user 🌱]($style) "
disabled = false
show_always = true

[time]
disabled = true
